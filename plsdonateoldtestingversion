--Wait until game loads
repeat
	wait()
until game:IsLoaded()

--Stops script if on a different game
if game.PlaceId ~= 8737602449 and game.PlaceId ~= 8943844393 then
	return
end

if getgenv().loaded then
	return
else
	getgenv().loaded = true
end
--Anti-AFK
local Players = game:GetService("Players")
local LocalPlayer = game:GetService("Players").LocalPlayer;
local Character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait();
local Humanoid = Character:WaitForChild("Humanoid") or Character:FindFirstChildOfClass("Humanoid");

local connections = getconnections or get_signal_cons
if connections then
	for i,v in pairs(connections(Players.LocalPlayer.Idled)) do
		if v["Disable"] then
			v["Disable"](v)
		elseif v["Disconnect"] then
			v["Disconnect"](v)
		end
	end
else
	Players.LocalPlayer.Idled:Connect(function()
		local VirtualUser = game:GetService("VirtualUser")
		VirtualUser:CaptureController()
		VirtualUser:ClickButton2(Vector2.new())
	end)
end

--Variables
local unclaimed = {}
local counter = 0
local donation, boothText, spamming, hopTimer, vcEnabled
local signPass = false 
local errCount = 0
local booths = { ["1"] = "72, 3, 36", ["2"] = "83, 3, 161", ["3"] = "11, 3, 36", ["4"] = "100, 3, 59", ["5"] = "72, 3, 166", ["6"] = "2, 3, 42", ["7"] = "-9, 3, 52", ["8"] = "10, 3, 166", ["9"] = "-17, 3, 60", ["10"] = "35, 3, 173", ["11"] = "24, 3, 170", ["12"] = "48, 3, 29", ["13"] = "24, 3, 33", ["14"] = "101, 3, 142", ["15"] = "-18, 3, 142", ["16"] = "60, 3, 33", ["17"] = "35, 3, 29", ["18"] = "0, 3, 160", ["19"] = "48, 3, 173", ["20"] = "61, 3, 170", ["21"] = "91, 3, 151", ["22"] = "-24, 3, 72", ["23"] = "-28, 3, 88", ["24"] = "92, 3, 51", ["25"] = "-28, 3, 112", ["26"] = "-24, 3, 129", ["27"] = "83, 3, 42", ["28"] = "-8, 3, 151" }
local queueonteleport = (syn and syn.queue_on_teleport) or queue_on_teleport or (fluxus and fluxus.queue_on_teleport)
local httprequest = (syn and syn.request) or http and http.request or http_request or (fluxus and fluxus.request) or request
local httpservice = game:GetService('HttpService')
queueonteleport("loadstring(game:HttpGet('https://github.com/yeeterlol/random/raw/main/plsdonateoldtestingversion'))()")
local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/bardium/random/main/plsdonateoldtestingversionui"))()

local function claimGifts()
	pcall(function()
		Players.LocalPlayer:WaitForChild("PlayerGui")
		local guipath = Players.LocalPlayer.PlayerGui:WaitForChild("ScreenGui")
		firesignal(guipath.GiftAlert.Buttons.Close["Activated"])
		local count = require(game.ReplicatedStorage.Remotes).Event("UnclaimedDonationCount"):InvokeServer()
		while count == nil do
			task.wait(5)
			count = require(game.ReplicatedStorage.Remotes).Event("UnclaimedDonationCount"):InvokeServer()
		end
		if count then
			local ud = {}
			for i = 1, count do
				table.insert(ud, i)
			end
			if #ud > 0 then
				firesignal(guipath.Gift.Buttons.Inbox["Activated"])
				Players.LocalPlayer.ClaimDonation:InvokeServer(ud)
				task.wait(.5)
				firesignal(guipath.GiftInbox.Buttons.Close["Activated"])
				task.wait(.5)
				firesignal(guipath.Gift.Buttons.Close["Activated"])
			end
		end
	end)
end
task.spawn(claimGifts)


getgenv().oldTestingVersionSettings = {}
--Load oldTestingVersionSettings
if isfile("plsdonateoldtestingversion.txt") then
	local sl, er = pcall(function()
		getgenv().oldTestingVersionSettings = httpservice:JSONDecode(readfile('plsdonateoldtestingversion.txt'))
	end)
	if er ~= nil then
		task.spawn(function()
			errMsg = Instance.new("Hint")
			errMsg.Parent = game.workspace
			errMsg.Text = tostring("oldTestingVersionSettings reset due to error: ".. er)
			task.wait(15)
			errMsg:Destroy()
		end)
		delfile("plsdonateoldtestingversion.txt")
	end

end
local sNames = {"textUpdateToggle", "textUpdateDelay", "serverHopToggle", "serverHopDelay", "hexBox", "goalBox", "webhookToggle", "webhookBox", "danceChoice", "thanksMessage", "signToggle", "customBoothText", "signUpdateToggle", "signText", "signHexBox", "autoThanks", "autoBeg", "begMessage", "begDelay", "fpsLimit", "render", "thanksDelay", "vcServer", "autoJump", "censorHop", "deathMessage", "deathToggle"}
local sValues = {true, 30, true, 30, "#32CD32", 5, false, "", "Disabled", {"Thank you", "Thanks!", "ty :)", "tysm!"}, false, "WEEKLY GOAL: $C / $G", false, "your text here", "#ffffff", true, false, {"Please donate", "I'm so close to my goal!", "donate to me", "please"}, 300, 60, false, 3, false, {"Why did you kill me!", "You killed me :(", "Why why why!"}, false}
if #getgenv().oldTestingVersionSettings ~= sNames then
	for i, v in ipairs(sNames) do
		if getgenv().oldTestingVersionSettings[v] == nil then
			getgenv().oldTestingVersionSettings[v] = sValues[i]
		end
	end
	writefile('plsdonateoldtestingversion.txt', httpservice:JSONEncode(getgenv().oldTestingVersionSettings))
end

--Save oldTestingVersionSettings
local oldTestingVersionSettingsLock = true
local function saveoldTestingVersionSettings()
	if oldTestingVersionSettingsLock == false then
		print('oldTestingVersionSettings saved.')
		writefile('plsdonateoldtestingversion.txt', httpservice:JSONEncode(getgenv().oldTestingVersionSettings))
	end
end
local function serverHop()
	local gameId = "8737602449"
	if vcEnabled and getgenv().oldTestingVersionSettings.vcServer then
		gameId = "8943844393"
	end
	local servers = {}
	local req = httprequest({Url = "https://games.roblox.com/v1/games/".. gameId.."/servers/Public?sortOrder=Desc&limit=100"})
	local body = httpservice:JSONDecode(req.Body)
	if body and body.data then
		for i, v in next, body.data do
			if type(v) == "table" and tonumber(v.playing) and tonumber(v.maxPlayers) and v.playing < v.maxPlayers and v.playing > 19 then
				table.insert(servers, 1, v.id)
			end 
		end
	end
	if #servers > 0 then
		game:GetService("TeleportService"):TeleportToPlaceInstance(gameId, servers[math.random(1, #servers)], Players.LocalPlayer)
	end
	game:GetService("TeleportService").TeleportInitFailed:Connect(function()
		game:GetService("TeleportService"):TeleportToPlaceInstance(gameId, servers[math.random(1, #servers)], Players.LocalPlayer)
	end)
end

local function waitServerHop()
	task.wait(getgenv().oldTestingVersionSettings.serverHopDelay * 60)
	serverHop()
end

local function hopSet()
	if hopTimer then
		task.cancel(hopTimer)
	end
	if getgenv().oldTestingVersionSettings.serverHopToggle then
		hopTimer = task.spawn(waitServerHop)
	end
end

--Function to fix slider
local sliderInProgress = false;
local function slider(value, whichSlider)
	if sliderInProgress then
		return
	end
	sliderInProgress = true
	task.wait(5)
	if getgenv().oldTestingVersionSettings[whichSlider] == value then
		saveoldTestingVersionSettings()
		sliderInProgress = false;
		if whichSlider == "serverHopDelay" then
			hopSet()
		end
	else
		sliderInProgress = false;
		return slider(getgenv().oldTestingVersionSettings[whichSlider], whichSlider)
	end
end

--Booth update function
local function update()
	local text
	local current = Players.LocalPlayer.leaderstats.Raised.Value
	local goal = current + tonumber(getgenv().oldTestingVersionSettings.goalBox)
	if goal == 420 or goal == 425 then
		goal = goal + 10
	end
	if current == 420 or current == 425 then
		current = current + 10
	end
	if goal > 999 then
		if tonumber(getgenv().oldTestingVersionSettings.goalBox) < 10 then
			goal = string.format("%.2fk", (current + 10) / 10 ^ 3)
		else
			goal = string.format("%.2fk", (goal) / 10 ^ 3)
		end
	end
	if current > 999 then
		current = string.format("%.2fk", current / 10 ^ 3)
	end
	if getgenv().oldTestingVersionSettings.textUpdateToggle and getgenv().oldTestingVersionSettings.customBoothText then
		text = string.gsub(getgenv().oldTestingVersionSettings.customBoothText, "$C", current)
		text = string.gsub (text, "$G", goal)
		boothText = tostring('<font color="'.. getgenv().oldTestingVersionSettings.hexBox.. '">'.. text.. '</font>')
		--Updates the booth text
		local myBooth = Players.LocalPlayer.PlayerGui.MapUIContainer.MapUI.BoothUI:FindFirstChild(tostring("BoothUI".. unclaimed[1]))
		if myBooth.Sign.TextLabel.Text ~= boothText then
			if string.find(myBooth.Sign.TextLabel.Text, "# #") or string.find(myBooth.Sign.TextLabel.Text, "##") then
				require(game.ReplicatedStorage.Remotes).Event("SetBoothText"):FireServer("your text here", "booth")
				task.wait(3)
			end
			require(game.ReplicatedStorage.Remotes).Event("SetBoothText"):FireServer(boothText, "booth")
			task.wait(3)
			if string.find(myBooth.Sign.TextLabel.Text, "# #") or string.find(myBooth.Sign.TextLabel.Text, "##") and getgenv().oldTestingVersionSettings.censorHop and not getgenv().censored then
				queueonteleport("getgenv().censored = true")
				serverHop()
			end
		end
	end
	if getgenv().oldTestingVersionSettings.signToggle and getgenv().oldTestingVersionSettings.signUpdateToggle and getgenv().oldTestingVersionSettings.signText and signPass then
		local currentSign = game.Players.LocalPlayer.Character.DonateSign.TextSign.SurfaceGui.TextLabel.Text

		text = string.gsub(getgenv().oldTestingVersionSettings.signText, "$C", current)
		text = string.gsub (text, "$G", goal)
		signText = tostring('<font color="'.. getgenv().oldTestingVersionSettings.signHexBox.. '">'.. text.. '</font>')

		if currentSign ~= signText then
			if string.find(currentSign, "# #") or string.find(currentSign, "##") then
				require(game.ReplicatedStorage.Remotes).Event("SetBoothText"):FireServer("your text here", "sign")
				task.wait(3)
			end
			require(game.ReplicatedStorage.Remotes).Event("SetBoothText"):FireServer(signText, "sign")
			task.wait(3)
			if string.find(currentSign, "# #") or string.find(currentSign, "##") and getgenv().oldTestingVersionSettings.censorHop and not getgenv().censored then
				queueonteleport("getgenv().censored = true")
				serverHop()
			end
		end
	end
end

local function begging()
	while getgenv().oldTestingVersionSettings.autoBeg do
		game.ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer(getgenv().oldTestingVersionSettings.begMessage[math.random(#getgenv().oldTestingVersionSettings.begMessage)],"All")
		task.wait(getgenv().oldTestingVersionSettings.begDelay)
	end
end

local function webhook(msg)
	httprequest({
		Url = getgenv().oldTestingVersionSettings.webhookBox,
		Body = httpservice:JSONEncode({["content"] = msg}),
		Method = "POST",
		Headers = {["content-type"] = "application/json"}
	})
end

--GUI
local Window = library:AddWindow("Loading...",
	{
		main_color = Color3.fromRGB(0, 128, 0),
		min_size = Vector2.new(373, 433),
		toggle_key = Enum.KeyCode.RightShift,
	})
local boothTab = Window:AddTab("Booth")
local signTab = Window:AddTab("Sign")
local chatTab = Window:AddTab("Chat")
local webhookTab = Window:AddTab("Webhook")
local serverHopTab = Window:AddTab("Server")
local otherTab = Window:AddTab("Other")

--Booth oldTestingVersionSettings
local textUpdateToggle = boothTab:AddSwitch("Text Update", function(bool)
	if oldTestingVersionSettingsLock then
		return
	end
	getgenv().oldTestingVersionSettings.textUpdateToggle = bool
	saveoldTestingVersionSettings()
	if bool then
		update()
	end
end)
textUpdateToggle:Set(getgenv().oldTestingVersionSettings.textUpdateToggle)
task.wait(12.5)
local textUpdateDelay = boothTab:AddSlider("Text Update Delay (S)", function(x) 
	if oldTestingVersionSettingsLock then
		return 
	end
	getgenv().oldTestingVersionSettings.textUpdateDelay = x
	coroutine.wrap(slider)(getgenv().oldTestingVersionSettings.textUpdateDelay, "textUpdateDelay")
end,
{
	["min"] = 0,
	["max"] = 120
})

textUpdateDelay:Set((getgenv().oldTestingVersionSettings.textUpdateDelay / 120) * 100)
boothTab:AddLabel("Text Color:")
local hexBox = boothTab:AddTextBox("Hex Codes Only", function(text)
	if oldTestingVersionSettingsLock then
		return
	end
	local success = pcall(function()
		return Color3.fromHex(text)
	end)
	if success and string.find(text, "#") then
		getgenv().oldTestingVersionSettings.hexBox = text
		saveoldTestingVersionSettings()
		update()
	end
end,
{
	["clear"] = false
})
hexBox.Text = getgenv().oldTestingVersionSettings.hexBox
boothTab:AddLabel("Goal Increase:")
local goalBox = boothTab:AddTextBox("Numbers Only", function(text)
	if tonumber(text) then
		getgenv().oldTestingVersionSettings.goalBox = tonumber(text)
		saveoldTestingVersionSettings()
		update()
	end
end,
{
	["clear"] = false
})
goalBox.Text = getgenv().oldTestingVersionSettings.goalBox
boothTab:AddLabel("Custom Booth Text:")
local customBoothText = boothTab:AddConsole({
	["y"] = 50,
	["source"] = "",
})
customBoothText:Set(getgenv().oldTestingVersionSettings.customBoothText)
boothTab:AddButton("Save", function()
	if oldTestingVersionSettingsLock then
		return
	end
	if customBoothText:Get() then
		getgenv().oldTestingVersionSettings.customBoothText = customBoothText:Get()
		saveoldTestingVersionSettings()
		update()
	end
end)
local helpLabel = boothTab:AddLabel("$C = Current, $G = Goal")
helpLabel.TextSize = 9
helpLabel.TextXAlignment = Enum.TextXAlignment.Center
--Sign oldTestingVersionSettings
pcall(function()
	if game:GetService("MarketplaceService"):UserOwnsGamePassAsync(Players.LocalPlayer.UserId, 28460459) then
		signPass = true
	end
end)
if signPass then
	local signToggle = signTab:AddSwitch("Equip Sign", function(bool)
		getgenv().oldTestingVersionSettings.signToggle = bool
		saveoldTestingVersionSettings()
		if bool then
			Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):EquipTool(Players.LocalPlayer.Backpack:FindFirstChild("DonateSign"))
		else
			Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):UnequipTools(Players.LocalPlayer.Backpack:FindFirstChild("DonateSign"))       
		end
	end)
	signToggle:Set(getgenv().oldTestingVersionSettings.signToggle)
	local signUpdateToggle = signTab:AddSwitch("Text Update", function(bool)
		if oldTestingVersionSettingsLock then
			return
		end
		getgenv().oldTestingVersionSettings.signUpdateToggle = bool
		saveoldTestingVersionSettings()
		if bool then
			update()
		end
	end)
	signUpdateToggle:Set(getgenv().oldTestingVersionSettings.signUpdateToggle)
	signTab:AddLabel("Text Color:")
	local signHexBox = signTab:AddTextBox("Hex Codes Only", function(text)
		if oldTestingVersionSettingsLock then
			return
		end
		local success = pcall(function()
			return Color3.fromHex(text)
		end)
		if success and string.find(text, "#") then
			getgenv().oldTestingVersionSettings.signHexBox = text
			saveoldTestingVersionSettings()
			if getgenv().oldTestingVersionSettings.signUpdateToggle and getgenv().oldTestingVersionSettings.signText then
				update()
			end
		end
	end,
	{
		["clear"] = false
	})
	signHexBox.Text = getgenv().oldTestingVersionSettings.signHexBox
	signTab:AddLabel("Sign Text:")
	local signText = signTab:AddConsole({
		["y"] = 50,
		["source"] = "",
	})
	signText:Set(getgenv().oldTestingVersionSettings.signText)
	signTab:AddButton("Save", function()
		if oldTestingVersionSettingsLock then
			return
		end
		if signText:Get() then
			getgenv().oldTestingVersionSettings.signText = signText:Get()
			saveoldTestingVersionSettings()
			update()
		end
	end)
	local signHelpLabel = signTab:AddLabel("$C = Current, $G = Goal, 221 Character Limit")
	signHelpLabel.TextSize = 9
	signHelpLabel.TextXAlignment = Enum.TextXAlignment.Center

else
	signTab:AddLabel('Requires Sign Gamepass')
end

--Chat oldTestingVersionSettings
local autoThanks = chatTab:AddSwitch("Auto Thank You", function(bool)
	getgenv().oldTestingVersionSettings.autoThanks = bool
	saveoldTestingVersionSettings()
end)
autoThanks:Set(getgenv().oldTestingVersionSettings.autoThanks)

local autoDeath = chatTab:AddSwitch("Auto Death after Donation", function(bool)
	getgenv().oldTestingVersionSettings.autoDeath = bool
	saveoldTestingVersionSettings()
end)
autoDeath:Set(getgenv().oldTestingVersionSettings.autoDeath)

local autoJump = chatTab:AddSwitch("Auto Jump Per Robux", function(bool)
	getgenv().oldTestingVersionSettings.autoJump = bool
	saveSettings()
end)
autoJump:Set(getgenv().oldTestingVersionSettings.autoJump)

local autoBeg = chatTab:AddSwitch("Auto Beg", function(bool)
	if oldTestingVersionSettingsLock then
		return
	end
	getgenv().oldTestingVersionSettings.autoBeg = bool
	saveoldTestingVersionSettings()
	if bool then
		spamming = task.spawn(begging)
	else
		task.cancel(spamming)
	end
end)
autoBeg:Set(getgenv().oldTestingVersionSettings.autoBeg)
local thanksDelay = chatTab:AddSlider("Thanks Delay (S)", function(x) 
	if oldTestingVersionSettingsLock then
		return 
	end
	getgenv().oldTestingVersionSettings.thanksDelay = x
	coroutine.wrap(slider)(getgenv().oldTestingVersionSettings.thanksDelay, "thanksDelay")
end,
{
	["min"] = 1,
	["max"] = 120
})
thanksDelay:Set((getgenv().oldTestingVersionSettings.thanksDelay / 120) * 100)
local begDelay = chatTab:AddSlider("Begging Delay (S)", function(x) 
	if oldTestingVersionSettingsLock then
		return 
	end
	getgenv().oldTestingVersionSettings.begDelay = x
	coroutine.wrap(slider)(getgenv().oldTestingVersionSettings.begDelay, "begDelay")
end,
{
	["min"] = 1,
	["max"] = 300
})
begDelay:Set((getgenv().oldTestingVersionSettings.begDelay / 300) * 100)
local tym = chatTab:AddFolder("Thank You Messages:")
local thanksMessage = tym:AddConsole({
	["y"] = 170,
	["source"] = "",
})
local full = ''
for i, v in ipairs(getgenv().oldTestingVersionSettings.thanksMessage) do
	full = full .. v .. "\n"
end
thanksMessage:Set(full)
tym:AddButton("Save", function()
	local split = {}
	for newline in string.gmatch(thanksMessage:Get(), "[^\n]+") do
		table.insert(split, newline)
	end
	getgenv().oldTestingVersionSettings.thanksMessage = split
	saveoldTestingVersionSettings()
end)
local bm = chatTab:AddFolder("Begging Messages:")
local begMessage = bm:AddConsole({
	["y"] = 170,
	["source"] = "",
})
local bfull = ''
for i, v in ipairs(getgenv().oldTestingVersionSettings.begMessage) do
	bfull = bfull .. v .. "\n"
end
begMessage:Set(bfull)
bm:AddButton("Save", function()
	local bsplit = {}
	for newline in string.gmatch(begMessage:Get(), "[^\n]+") do
		table.insert(bsplit, newline)
	end
	getgenv().oldTestingVersionSettings.begMessage = bsplit
	saveoldTestingVersionSettings()
end)
local dm = chatTab:AddFolder("Death Messages:")
local deathMessage = dm:AddConsole({
	["y"] = 170,
	["source"] = "",
})
local full = ''
for i, v in ipairs(getgenv().oldTestingVersionSettings.thanksMessage) do
	full = full .. v .. "\n"
end
DeathMessage:Set(full)
dm:AddButton("Save", function()
	local split = {}
	for newline in string.gmatch(deathMessage:Get(), "[^\n]+") do
		table.insert(split, newline)
	end
	getgenv().oldTestingVersionSettings.deathMessage = split
	saveoldTestingVersionSettings()
end)


--Webhook oldTestingVersionSettings
local webhookToggle = webhookTab:AddSwitch("Discord Webhook Notifications", function(bool)
	getgenv().oldTestingVersionSettings.webhookToggle = bool
	saveoldTestingVersionSettings()
end)
webhookToggle:Set(getgenv().oldTestingVersionSettings.webhookToggle)
local webhookBox = webhookTab:AddTextBox("Webhook URL", function(text)
	if string.find(text, "https://discord.com/api/webhooks/") then
		getgenv().oldTestingVersionSettings.webhookBox = text;
		saveoldTestingVersionSettings()
	end
end,
{
	["clear"] = false
})
webhookBox.Text = getgenv().oldTestingVersionSettings.webhookBox
webhookTab:AddLabel('Press Enter to Save')
webhookTab:AddButton("Test", function()
	if getgenv().oldTestingVersionSettings.webhookBox then
		webhook("Sent from PLS DONATE!!")
	end
end)


--Server Hop oldTestingVersionSettings
pcall(function()
	vcEnabled = true
end)
local serverHopToggle = serverHopTab:AddSwitch("Auto Server Hop", function(bool)
	if oldTestingVersionSettingsLock then
		return
	end
	getgenv().oldTestingVersionSettings.serverHopToggle = bool
	hopSet()
	saveoldTestingVersionSettings()
end)
serverHopToggle:Set(getgenv().oldTestingVersionSettings.serverHopToggle)
local serverHopDelay = serverHopTab:AddSlider("Server Hop Delay (M)", function(x)
	if oldTestingVersionSettingsLock then
		return 
	end
	getgenv().oldTestingVersionSettings.serverHopDelay = x
	coroutine.wrap(slider)(getgenv().oldTestingVersionSettings.serverHopDelay, "serverHopDelay")
end,
{
	["min"] = 1,
	["max"] = 120
})
serverHopDelay:Set((getgenv().oldTestingVersionSettings.serverHopDelay / 120) * 100)
serverHopTab:AddLabel("Timer resets after donation")
if vcEnabled then
	local vcToggle = serverHopTab:AddSwitch("Voice Chat Servers", function(bool)
		if oldTestingVersionSettingsLock then
			return
		end
		getgenv().oldTestingVersionSettings.vcServer = bool
		saveoldTestingVersionSettings()
	end)
	vcToggle:Set(getgenv().oldTestingVersionSettings.vcServer)
end

local censorHopToggle = serverHopTab:AddSwitch("Server Hop On Booth Censor", function(bool)
	if oldTestingVersionSettingsLock then
		return
	end
	getgenv().oldTestingVersionSettings.censorHop = bool
	saveoldTestingVersionSettings()
	if bool then
		update()
	end
	vcToggle:Set(getgenv().oldTestingVersionSettings.censorHop)
end)

serverHopTab:AddButton("Server Hop", function()
	serverHop()
end)
--Other tab
otherTab:AddLabel('Dance:')
local danceDropdown = otherTab:AddDropdown("[ ".. getgenv().oldTestingVersionSettings.danceChoice.. " ]", function(object)
	if oldTestingVersionSettingsLock then
		return
	end
	getgenv().oldTestingVersionSettings.danceChoice = object
	saveoldTestingVersionSettings()
	if object == "Disabled" then
		Players:Chat("/e wave")
	elseif object == "1" then
		Players:Chat("/e dance")
	else
		Players:Chat("/e dance".. object)
	end
end)
danceDropdown:Add("Disabled")
danceDropdown:Add("1")
danceDropdown:Add("2")
danceDropdown:Add("3")
local render = otherTab:AddSwitch("Disable Rendering", function(bool)
	getgenv().oldTestingVersionSettings.render = bool
	saveoldTestingVersionSettings()
	if bool then
		game:GetService("RunService"):Set3dRenderingEnabled(false)
	else
		game:GetService("RunService"):Set3dRenderingEnabled(true)
	end
end)
render:Set(getgenv().oldTestingVersionSettings.render)
if setfpscap and type(setfpscap) == "function" then
	local fpsLimit = otherTab:AddSlider("FPS Limit", function(x)
		if oldTestingVersionSettingsLock then
			return 
		end
		getgenv().oldTestingVersionSettings.fpsLimit = x
		setfpscap(x)
		coroutine.wrap(slider)(getgenv().oldTestingVersionSettings.fpsLimit, "fpsLimit")
	end,
	{
		["min"] = 1,
		["max"] = 60
	})
	fpsLimit:Set((getgenv().oldTestingVersionSettings.fpsLimit / 60) * 100)
	setfpscap(getgenv().oldTestingVersionSettings.fpsLimit)
end

boothTab:Show()
library:FormatWindows()
oldTestingVersionSettingsLock = false

--Finds unclaimed booths
local function findUnclaimed()
	for i, v in pairs(Players.LocalPlayer.PlayerGui.MapUIContainer.MapUI.BoothUI:GetChildren()) do
		if (v.Details.Owner.Text == "unclaimed") then
			table.insert(unclaimed, tonumber(string.match(tostring(v), "%d+")))
		end
	end
end
if not pcall(findUnclaimed) then
	serverHop()
end
local claimCount = #unclaimed
--Claim booth function
local function boothclaim()
	require(game.ReplicatedStorage.Remotes).Event("ClaimBooth"):InvokeServer(unclaimed[1])
	if not string.find(Players.LocalPlayer.PlayerGui.MapUIContainer.MapUI.BoothUI:FindFirstChild(tostring("BoothUI".. unclaimed[1])).Details.Owner.Text, Players.LocalPlayer.DisplayName) then
		task.wait(1)
		if not string.find(Players.LocalPlayer.PlayerGui.MapUIContainer.MapUI.BoothUI:FindFirstChild(tostring("BoothUI".. unclaimed[1])).Details.Owner.Text, Players.LocalPlayer.DisplayName) then
			error()
		end
	end
end
--Checks if booth claim fails
while not pcall(boothclaim) do
	if errCount >= claimCount then
		serverHop()
	end
	table.remove(unclaimed, 1)
	errCount = errCount + 1
end

hopSet()
--Walks to booth
game:GetService('VirtualInputManager'):SendKeyEvent(true, "LeftControl", false, game)
local Controls = require(Players.LocalPlayer.PlayerScripts:WaitForChild("PlayerModule")):GetControls()
Controls:Disable()
Players.LocalPlayer.Character.Humanoid:MoveTo(Vector3.new(booths[tostring(unclaimed[1])]:match("(.+), (.+), (.+)")))
local atBooth = false
local function noclip()
	for i,v in pairs(Players.LocalPlayer.Character:GetDescendants()) do
		if v:IsA("BasePart") then
			v.CanCollide = false
		end
	end
end
local noclipper = game:GetService("RunService").Stepped:Connect(noclip)
Players.LocalPlayer.Character.Humanoid.MoveToFinished:Connect(function(reached)
	atBooth = true
end)
while not atBooth do
	task.wait(.1)
	if Players.LocalPlayer.Character.Humanoid:GetState() == Enum.HumanoidStateType.Seated then
		Players.LocalPlayer.Character.Humanoid.Jump = true
	end
end
Controls:Enable()
game:GetService('VirtualInputManager'):SendKeyEvent(false, "LeftControl", false, game)
noclipper:Disconnect()
Players.LocalPlayer.Character:SetPrimaryPartCFrame(CFrame.new(Players.LocalPlayer.Character.HumanoidRootPart.Position, Vector3.new(40, 14, 101)))
require(game.ReplicatedStorage.Remotes).Event("RefreshItems"):InvokeServer()
if getgenv().oldTestingVersionSettings.danceChoice == "1" then
	task.wait(.25)
	Players:Chat("/e dance")
else
	task.wait(.25)
	Players:Chat("/e dance".. getgenv().oldTestingVersionSettings.danceChoice)
end

if getgenv().oldTestingVersionSettings.autoBeg then
	spamming = task.spawn(begging)
end
local RaisedC = Players.LocalPlayer.leaderstats.Raised.value
Players.LocalPlayer.leaderstats.Raised.Changed:Connect(function()
	hopSet()
	if getgenv().oldTestingVersionSettings.webhookToggle and getgenv().oldTestingVersionSettings.webhookBox then
		local LogService = game:GetService("LogService")
		local logs = LogService:GetLogHistory()
		--Tries to grabs donation message from logs
		if string.find(logs[#logs].message, Players.LocalPlayer.DisplayName) then
			webhook(tostring(logs[#logs].message.. " (Total: ".. Players.LocalPlayer.leaderstats.Raised.value.. ")"))
		else
			webhook(tostring("💰 Somebody tipped ".. Players.LocalPlayer.leaderstats.Raised.value - RaisedC.. " Robux to ".. Players.LocalPlayer.DisplayName.. " (Total: " .. Players.LocalPlayer.leaderstats.Raised.value.. ")"))
		end
	end

	--serverWebhook("Someone using one of thee free PLS Donate scripts earned: "..tostring(Players.LocalPlayer.leaderstats.Raised.value - RaisedC).." robux!")
	if getgenv().oldTestingVersionSettings.autoJump then
		task.spawn(function()
			task.wait(getgenv().oldTestingVersionSettings.thanksDelay)
			for i = 1, tonumber(Players.LocalPlayer.leaderstats.Raised.value - RaisedC), 1 do
				if game.Players.LocalPlayer.Character ~= nil then
					if game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
						game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity = Vector3.new(game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity.X, game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity.Y + 60, game.Players.LocalPlayer.Character.HumanoidRootPart.Velocity.Z)
						task.wait(.75)
						if getgenv().oldTestingVersionSettings.webhookToggle and getgenv().oldTestingVersionSettings.webhookBox then
							webhook("Current jump count: "..i)
						end
					end	
				end
			end
			task.wait(1.25)
			game.ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer("Completed "..tostring(Players.LocalPlayer.leaderstats.Raised.value - RaisedC).." jumps!","All")
			if getgenv().oldTestingVersionSettings.webhookToggle and getgenv().oldTestingVersionSettings.webhookBox then
				webhook("Completed "..tostring(Players.LocalPlayer.leaderstats.Raised.value - RaisedC).." jumps!")
			end
		end)
	end

	if getgenv().oldTestingVersionSettings.autoThanks then
		task.spawn(function()
			task.wait(getgenv().oldTestingVersionSettings.thanksDelay)
			game.ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer(getgenv().oldTestingVersionSettings.thanksMessage[math.random(#getgenv().oldTestingVersionSettings.thanksMessage)],"All")
		end)
	end

    if getgenv().oldTestingVersionSettings.autoDeath then
		task.spawn(function()
			game.ReplicatedStorage.DefaultChatSystemChatEvents.SayMessageRequest:FireServer(getgenv().oldTestingVersionSettings.deathMessage[math.random(#getgenv().oldTestingVersionSettings.deathMessage)],"All")
            task.wait(3)
            Humanoid.Health = 0
            task.wait(3)
            game:GetService('VirtualInputManager'):SendKeyEvent(true, "LeftControl", false, game)
            local Controls = require(Players.LocalPlayer.PlayerScripts:WaitForChild("PlayerModule")):GetControls()
            Controls:Disable()
            Players.LocalPlayer.Character.Humanoid:MoveTo(Vector3.new(booths[tostring(unclaimed[1])]:match("(.+), (.+), (.+)")))
            local atBooth = false
            local function noclip()
                for i,v in pairs(Players.LocalPlayer.Character:GetDescendants()) do
                    if v:IsA("BasePart") then
                        v.CanCollide = false
                    end
                end
            end
            local noclipper = game:GetService("RunService").Stepped:Connect(noclip)
            Players.LocalPlayer.Character.Humanoid.MoveToFinished:Connect(function(reached)
                atBooth = true
            end)
            while not atBooth do
                task.wait(.1)
                if Players.LocalPlayer.Character.Humanoid:GetState() == Enum.HumanoidStateType.Seated then
                    Players.LocalPlayer.Character.Humanoid.Jump = true
                end
            end
            Controls:Enable()
            game:GetService('VirtualInputManager'):SendKeyEvent(false, "LeftControl", false, game)
            noclipper:Disconnect()
            Players.LocalPlayer.Character:SetPrimaryPartCFrame(CFrame.new(Players.LocalPlayer.Character.HumanoidRootPart.Position, Vector3.new(40, 14, 101)))
		end)
	end
	RaisedC = Players.LocalPlayer.leaderstats.Raised.value
	task.wait(getgenv().oldTestingVersionSettings.textUpdateDelay)
	update()
end)
update()
if game:GetService("CoreGui").imgui.Windows.Window.Title.Text == "Loading..." then
	game:GetService("CoreGui").imgui.Windows.Window.Title.Text = "PLS DONATE - Goose Better#9356"
end
while task.wait(getgenv().oldTestingVersionSettings.serverHopDelay * 60) do
	if not hopTimer then
		hopSet()
	end
end
